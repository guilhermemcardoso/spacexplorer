import React from 'react';
import {
  SafeAreaView,
} from 'react-native';
import LaunchList from '../components/launchList';
const Home = () => {

  return (
    <SafeAreaView>
      <LaunchList />
    </SafeAreaView>
  );
};

export default Home;
